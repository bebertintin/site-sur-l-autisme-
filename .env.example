# Configuration de l'environnement
# Copier ce fichier en .env et adapter les valeurs avant déploiement.
# (Le backend charge automatiquement .env via dotenv)

# Serveur
PORT=3000

# Email (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=votre-adresse@gmail.com
SMTP_PASS=votre-password-app
EMAIL_FROM=noreply@example.com
ADMIN_EMAIL=admin@example.com

# Authentification administrateur (optionnel)
ADMIN_USER=admin
ADMIN_PASS=motdepasse123

# URL publique du site (pour les liens dans les e-mails)
PUBLIC_URL=https://votre-site.com
